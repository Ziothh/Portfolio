---
// Component Imports
import Button from '../components/Button.astro';

import MainLayout from "../layouts/main.astro"

export const layout = "../layouts/main.astro"

import Section from '../layouts/Section.astro';

const sections = await Astro.glob<{id: string}>('../sections/home/*.section.mdx')


// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---
<!-- {console.log(PROJECTS.map(p => p.frontmatter)) ?? null} -->
<MainLayout>
    {sections.map(s => (
        <Section id={s.frontmatter.id}>
            <s.Content/>
        </Section>
    ))}

    <!-- {PROJECTS.map(p => p.frontmatter.name)} -->
        <!-- {PROJECTS.map(p => (
        <pre>{JSON.stringify(p, null, 4)}</pre> 
    ))} -->
</MainLayout>
